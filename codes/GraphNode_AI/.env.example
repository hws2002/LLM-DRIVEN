# ================================================================
# GraphNode_AI — text text text
# text text .env text text (.envtext gitignore text)
# text text text shared/config.py text text
# ================================================================

# ── LLM API text ──────────────────────────────────────────────────
DEV_OPENAI_API_KEY=
DEV_ZAI_API_KEY=
DEV_GROQ_API_KEY=
DEV_OPENROUTER_API_KEY=

# ── DB text ──────────────────────────────────────────────────
NEO4J_password=
NEO4J_password_dev=
CHROMA_API_KEY=
CHROMA_API_KEY_DEV=
MONGODB_URL=mongodb+srv://user:pass@cluster.mongodb.net/

# ── text AWS text text (ECS text text text → IAM role text text) ──
AWS_PROFILE=       # text text text text

# ── text text text DB text (config.py text prod) ─────────
# dev DB text text text text
CHROMA_DATABASE=TACO_GraphNode_DEV
NEO4J_URI=        # dev neo4j URI
NEO4J_USER=       # dev neo4j user
NEO4J_password=   # dev neo4j password

# ── text text text LLM text (text) ────────────────────────
# text : microtext openai text text..
# MICROSCOPE_LLM_PROVIDER=openai
# MICROSCOPE_LLM_MODEL=gpt-4o-mini

